[[ TERM_PROGRAM == "iTerm.app" ]] && \
  [ -e "$HOME/.iterm2_shell_integration.$shell" ] && \
    source "$HOME/.iterm2_shell_integration.$shell"

export BASH_SILENCE_DEPRECATION_WARNING=1

alias flushdns='sudo killall -HUP mDNSResponder'
