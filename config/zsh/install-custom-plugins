# We check to make sure OhMyZsh! is initialized first.
if [[ ${ZSH_CUSTOM} ]]; then
  [[ ! -d ${ZSH_CUSTOM}/plugins/zsh-autosuggestions ]] && \
    git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM}/plugins/zsh-autosuggestions
fi
